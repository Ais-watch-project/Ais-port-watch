# Secrets — never commit these
.env

# Local-only compose overrides (e.g. port remaps for machine-specific conflicts)
docker-compose.override.yml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Data — raw dumps and databases stay off GitHub
data/
*.db
*.sqlite
*.csv
*.parquet

# Editors / OS
.vscode/
.idea/
.DS_Store
